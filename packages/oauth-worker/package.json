{
  "name": "@slack-task-mcp/oauth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers OAuth server for Slack Task MCP",
  "type": "module",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
